numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
tqdm>=4.65.0
pyyaml>=6.0
# Lexical BM25-Okapi scorer for Stage 2b hybrid retrieval (pure-CPU, no GPU).
rank-bm25>=0.2.2
# Used for offline embedding pre-computation (precompute_embeddings.py) and the
# optional cross-encoder re-rank in rank.py. The dense-cosine ranking itself is
# pure NumPy; torch/sentence-transformers are only needed when (re)generating
# embeddings or running the cross-encoder.
sentence-transformers>=2.7.0
torch>=2.0.0
